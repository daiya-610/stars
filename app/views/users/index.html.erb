<div class="text-center">
<h1>ユーザー一覧</h1>

<%= will_paginate %>

<ul class="users_index">
  <% @users.each do |user| %>
    <li>
      <%= gravatar_for user, size: 70%>
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>

<%= will_paginate %>
</div>